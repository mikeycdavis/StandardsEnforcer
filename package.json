{
  "name": "standards-enforcer",
  "version": "0.6.0",
  "description": "Determine which independent standards releases govern a repository, pin them by immutable identity, run their official evaluators, and prevent a required standard from being silently absent or bypassed.",
  "type": "module",
  "bin": {
    "standards-enforce": "scripts/enforce.mjs"
  },
  "scripts": {
    "test": "node scripts/test-surface.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED",
  "private": true
}
