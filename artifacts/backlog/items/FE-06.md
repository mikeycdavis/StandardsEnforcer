---
id: FE-06
type: feature
title: Decide which line becomes authoritative, and then the disposition of main
parent: EP-04
status: COMPLETE
opened: 2026-08-11
closed: 2026-08-16
evidence:
  - 3ac4431
  - ae05c99
  - 4370a44
  - artifacts/evidence/2026-08-16-composition-topology-measurement.md
tags: [release, branches, reconciliation]
---

## Decision 1 of 2 — the authoritative continuation, 2026-08-13

**`reconciliation/m3-corrected` at `3ac4431` is the authoritative continuation of the M3 line.**
Decided by the owner on measured evidence. This item stays open because it carries a second decision:
the disposition of `main`, which is a **consequence** of this one and deliberately not folded into it.

### The measurement this rests on

Taken 2026-08-13, live, no fetch:

```text
main                           505045abf1705992d04c5ba4c4f70c1c453a62bc
m3-scope-registry              b61841f19b7c07179e311453f5e0cf29e9398310
reconciliation/m3-corrected    3ac44311bb0d8b7984c24c345737db3e999bc3b2   ← authoritative
reconciliation/m3-integration  65445f3a6d042f98f7a5e2e451d224ecea8f3850

main → m3-scope-registry         0 ahead / 23 behind   main is a strict ancestor
main → m3-corrected              0 ahead / 24 behind   main is a strict ancestor
m3-scope-registry → m3-corrected 0 ahead /  1 behind   exactly the reconciliation commit
m3-corrected ↔ m3-integration    3 ahead / 19 behind   diverged
```

**Remote topology was not established** — no fetch was performed, so `origin/*` figures are as of an
unknown last fetch and were not used in this decision. Nothing here depends on them.

### Why `3ac4431` over `b61841f`

`3ac4431` **is** `b61841f` plus one commit. Nothing on `m3-scope-registry` is given up, and what is
added is the reconciliation itself: FE-15 with its remedy and evidence, FE-14 under the assurance
parent its own acceptance property implies, the M2 supersession records, and the recorded D3
exclusion. Choosing `b61841f` would be knowingly choosing the pre-reconciliation account and
discarding the reason the reconciliation was performed.

### Why not `reconciliation/m3-integration`

Not because its work is poor — it is not; FE-16 in particular is real and valuable. Because:

- it still carries **this item's superseded two-branch framing** and its stale commit counts, verified
  by measurement on 2026-08-13: FE-06 is byte-identical between `m3-scope-registry` and
  `m3-corrected`, and **differs** on the integration line;
- it has accumulated 19 commits beyond the pinned reconciliation source and diverges from the
  corrected candidate by 3;
- its post-`670d617` work is **input to a second reconciliation**, measured separately, and is not a
  reason to bypass the first.

### What this decision does not do

- **It does not move `main`.** That is decision 2, and its justification must read *follow the
  established authority* rather than *this branch is 24 commits ahead*.
- **It does not import FE-16**, adjudicate EP-01, or correct ST-07's blocking record. Each is a
  separate act on the now-authoritative line.
- **It is not a push, and not a promotion of anything to a remote.** Re-measure immediately before any
  `main` movement or push; this measurement is evidence about 2026-08-13 and nothing later.

### One consequence to settle before decision 2

Backlog authority now lives on this line, so items may legitimately diverge from
`m3-scope-registry` — the byte-identity that made this item safe to read from either candidate ends
here, by design. What remains open is whether `m3-scope-registry` is fast-forwarded to the
authoritative commit or retired as a name. Until that is settled, **a decision recorded on
`m3-scope-registry` is not authoritative**, and this is the only place that says so.

## Decision 2 of 2 — the disposition of `main`, 2026-08-13

**`main` was fast-forwarded to `634f0ec` and pushed.** Decided by the owner. The justification is
*follow the established authority* — decision 1 already ruled which line is authoritative, and `main`
was a strict ancestor of it — **not** *this branch is further ahead*.

### The falsifiers this rests on, measured immediately before the act

Stated as propositions rather than as a narrative, so that a later reader can see the operation was
gated rather than adapted. Any one of them false was specified to stop the operation:

```text
authoritative tip == 634f0ec                                    held
authoritative worktree clean                                    held
local main == 505045a                                           held
remote refs/heads/main == 505045a  (ls-remote, no fetch)         held
505045a is an ancestor of 634f0ec                               held
```

The push reported `505045a..634f0ec`, a fast-forward — no force, no history rewritten, and `main`
was checked out in no worktree, so no tree was touched. **`main` only** was pushed;
`reconciliation/m3-corrected` remains unpublished, and the remote still carries exactly two branches.

### What this decision does not do

- **It does not incorporate or adjudicate the M5 evidence.** `enforcement` at `fa0fca8` remains the
  **sole measured holder** of `artifacts/evidence/2026-08-10-m5-dormant.md`,
  `artifacts/evidence/2026-08-10-m5-protocol.md` and `WORKTREES.md`. Re-measured 2026-08-13: those
  three paths exist on no other line — not on the authoritative line, not on
  `reconciliation/m3-integration`.

  > **Promoting `634f0ec` to `main` does not incorporate or adjudicate that evidence and must not be
  > read as establishing that M5 never existed or was abandoned.**

  Whether rescuing it is required repository work is a **separate decision**, deliberately not taken
  here and deliberately not converted into a backlog item by the mere fact of the observation. A
  limitation observed is not work manufactured.
- **It does not decide `m3-scope-registry`.** That name is still live, still published, and now two
  commits behind the authority — a reader could mistake it for current. Undecided, by design.
- **It does not absorb `reconciliation/m3-integration`.** FE-16, FE-17 and the later ADRs deserve
  their own measurement and adjudication. Waiting for a line that moved three times on 2026-08-13
  would let a live foreign branch indefinitely block the default branch, which recreates the
  condition this item exists to end.
- **It does not adjudicate EP-01 or correct ST-07.** Both are independently actionable and neither
  was touched.

### The lines held separate after this decision

```text
main disposition               resolved here
m3-scope-registry disposition  undecided
M5 evidence rescue             separately adjudicated — not yet a decision, not yet an item
second reconciliation          separately measured
EP-01                          independently actionable
ST-07                          independently actionable
```

## Decision 3 — the branch-name disposition, 2026-08-16

**Decisions 1 and 2 are not re-decided here, and neither is amended.** Decision 2 above was written
on 2026-08-13 on `reconciliation/m3-corrected` as `ae05c99` and had never reached `main`. It is
**imported verbatim**, as its own commit, with its original message, author and date. Its value is
that it records the act when the act occurred; folding it into today's text or restating it as
current would destroy exactly that. The same applies to
[`2026-08-16-composition-topology-measurement.md`](../../evidence/2026-08-16-composition-topology-measurement.md),
carried as `4370a44` rather than copied, because the fact that it was recorded independently on that
line *before* today's disposition is provenance worth keeping.

Until this import, the authoritative branch could not explain how it became authoritative without
reading a divergent local worktree. That is the split account this item exists to eliminate, and it
had quietly reappeared in the record of the item's own resolution.

### The topology, re-measured today

Decision 1 required re-measurement immediately before any `main` movement, on the grounds that its
figures were evidence about 2026-08-13 and nothing later. Measured 2026-08-16 against `main` at
`327bfbb`, with `ls-remote` for every remote figure:

```text
remote refs/heads/main                        327bfbb
remote refs/heads/m3-scope-registry           e96d90c
remote refs/heads/feature/adapter-contract    47f5eec
remote tags                                   none

ref                             ancestor of main?  main ahead/behind  merge base
3ac4431  (decision 1's subject)  YES                13 / 0             3ac4431
e96d90c  (remote scope-registry) YES                28 / 0             e96d90c
b61841f  (local scope-registry)  YES                14 / 0             b61841f
reconciliation/m3-corrected      no                 12 / 2             634f0ec
reconciliation/m3-integration    no                 16 / 24            7df5e69
feature/adapter-contract-1.1.0   no                  4 / 3             bf186e2
```

**`main` at `327bfbb` is the authoritative continuation.** Not because it is furthest ahead — that is
the reasoning both earlier decisions refused — but because it *contains* `3ac4431`, the commit
Decision 1 named, as a strict ancestor and extends it. The line declared authoritative and the
default branch are no longer two things.

**Decision 2's half is closed as already realised, not as a new promotion.** Nothing was pushed to
`main` to settle it. The fast-forward `ae05c99` records happened on 2026-08-13, and every merge since
has extended it.

**The name `reconciliation/m3-corrected` no longer denotes `3ac4431`.** It has moved two commits onto
a path that is not in `main` — and both of those commits are the ones imported here. This is the
argument for pinning commits and retiring names, made by the item's own subject.

### `m3-scope-registry` — retired as a remote name

The remote ref was deleted on 2026-08-16. Falsifiers measured immediately before the act, each
specified to stop it:

```text
remote refs/heads/m3-scope-registry == e96d90c                    held
e96d90c is an ancestor of main (327bfbb)                          held
remote refs/heads/main == 327bfbb                                 held
local m3-scope-registry (b61841f) is an ancestor of main           held
m3-scope-registry is checked out in another worktree               held — so it was not touched
```

`git push origin --delete m3-scope-registry` reported `- [deleted] m3-scope-registry`. The remote now
carries two branches: `main` and `feature/adapter-contract-1.1.0`.

**No commit was discarded.** `e96d90c` is a strict ancestor of `main`, so the ref is exactly
restorable with `git push origin e96d90c:refs/heads/m3-scope-registry` should anyone want the name
back. What was removed is a published ref 28 commits behind the authority, which a reader could
mistake for a live alternate continuation — the dual-line ambiguity this item was created to end.

**Nothing local was touched, deliberately.** `m3-scope-registry` at `b61841f` remains checked out in
`F:/Repos/StandardsEnforcer`, and `reconciliation/m3-corrected` and `reconciliation/m3-integration`
remain checked out in their own worktrees. They are historical evidence and they exist durably; no
remote branch was recreated to preserve a name. Deleting a branch checked out in another worktree
would also cross a boundary this item has no reason to cross.

### The conflict this item existed to adjudicate is gone

Established by the imported measurement, not asserted here: 34 distinct item ids across the four
candidate lines, 25 identical, and every one of the 9 rows that differ is an **absence** rather than
a contradiction. No two lines assert different statuses for the same item, and EP-01 — which was the
substance of the original conflict — now reports identically on all four.

> What remains across the lines is unincorporated work, not contradictory work.

That distinction is what makes closing this item honest. FE-06 was never *"merge everything"*; it was
*"decide which line is authoritative, and stop the backlog from having two accounts"*.

### Closed 2026-08-16

Four conditions, each established above rather than asserted:

```text
main is the authoritative continuation              3ac4431 is an ancestor; main extends it
the main-promotion record is present on main        ae05c99 imported verbatim
no unresolved conflicting backlog account remains   9 differences, all absence, none contradiction
no stale remote ref presents itself as a rival      m3-scope-registry retired; two branches remain
```

**What this closure does not do**, each held open exactly as Decision 2 left it:

- **It does not close [EP-04](./EP-04.md).** This item is its only child, so the epic now reads
  `(1/1)`, and that is a count rather than a disposition. EP-04's acceptance claim is broader, in the
  same way EP-01's was, and a parent that closes as a side effect of arithmetic is the failure this
  repository's backlog machinery exists to prevent.
- **It does not rescue the M5 evidence.** `artifacts/evidence/2026-08-10-m5-dormant.md`,
  `2026-08-10-m5-protocol.md` and `WORKTREES.md` remain on `enforcement` at `fa0fca8` and on no other
  line, `main` included — re-verified 2026-08-16 with `git cat-file` against both. Decision 2 declined
  to convert that observation into work, and nothing in today's measurement changes the proposition.
- **It does not absorb `reconciliation/m3-integration`.** It is 24 commits ahead of `main` and holds
  items existing nowhere else. Two record commits crossed from `m3-corrected` because they document
  `main`'s own authority; that is repatriation, not incorporation, and it establishes no precedent
  for the second reconciliation.
- **It does not adjudicate EP-01 or correct ST-07.** Untouched, and independently actionable.

## What

Decide which development line is authoritative for this repository, reconcile the backlog claims that
disagree across lines, and only then decide what `main` should say.

**Re-scoped 2026-08-12.** This item was written as a two-branch question — `m3-scope-registry` versus
`main` — and that framing is now known to be wrong. A third line exists and is ahead of both, and the
lines disagree about a backlog claim rather than merely differing in commits. Publishing either side
before that disagreement is adjudicated would make branch movement decide a question nobody decided.

## Why

`main` describes an earlier repository than any working line. That was the original observation and it
remains true. What has changed is that "the working line" is no longer singular, so *choose what
`main` says* now contains a prior question: **which account is the record?**

The disagreement is substantive, not clerical. One line holds a parent epic closed; another holds the
same epic with open children carrying real evidence. A merge in either direction produces a backlog
that fails validation, and resolving it by whichever direction is merged second is not a decision.

## The durable requirement

This item is normative about **procedure**, not about any measurement. In order:

1. **Measure the live topology at decision time.** Current branch and full HEAD, working tree state,
   every local and remote ref, ahead/behind against each materially relevant line, worktrees and what
   they hold, and whether commits appeared since the last recorded checkpoint. A previously recorded
   count is evidence about that earlier moment and nothing else.
2. **Fetch before any remote claim.** Remote-tracking refs are stale until fetched. Ahead/behind
   against `origin/*` is not established by a measurement that did not fetch, and must not be reported
   as though it were.
3. **Identify every materially relevant line**, including any that this item does not name. The
   failure this re-scope corrects was reasoning from the two lines the item happened to mention.
4. **Reconcile conflicting backlog claims first.** Where two lines disagree about an item's status,
   adjudicate the claim on its evidence before choosing a branch. Never let merge order decide it.
5. **Then decide `main`.** Only once the resulting backlog is internally consistent.

**No ahead/behind count belongs in this body.** The original text carried one and it has been wrong
three times — see the evidence section. A number written into a decision item decays into an
assumption that outlives its measurement.

## Notes

Written as a decision rather than as "merge to main", because nothing in the repository establishes
that a merge is intended. The observable fact is divergence; the disposition is a judgement the owner
has not recorded. That remains true and is the reason this item exists at all.

There is exactly one merge commit in this repository's history (`bc118b1`, a branch merge with no
pull-request reference), re-verified 2026-08-12. This line was not developed through pull requests, so
`backlog-reconcile` reports its two pull-request-dependent checks as **NOT RUN** rather than as
passing. Whether this backlog agrees with what merged here is undetermined, not verified. A zero-merge
repository is not a reconciled one.

**Closing this item is not production readiness.** ST-07 is independent: there is still no
live-validated authoritative GitHub Actions enforcement root, and no branch disposition creates one.

## Dated evidence — the measurement of 2026-08-12, not the decision model

Recorded so the re-scope is auditable. **Re-measure before use; do not reason from these numbers.**

```text
HEAD                           2a2a4fed4f0722674c6e8d8920575ce749e6bf5b  (m3-scope-registry, clean)
main                           505045abf1705992d04c5ba4c4f70c1c453a62bc  (== origin/main)
origin/m3-scope-registry       e96d90cb096319924aa6a5d12350414663456726  (unfetched — see below)
reconciliation/m3-integration  ac668fefcabf7146e17a6cef1501f2c3e0cdd59c  (no remote ref)
```

| Pair | Ahead | Behind | Relationship |
|---|---:|---:|---|
| `m3` vs `origin/m3` | 13 | 0 | fast-forwardable |
| `m3` vs `main` | 22 | 0 | `main` is a strict ancestor |
| `m3-integration` vs `m3` | 13 | 1 | diverged at `7df5e69`; the 1 is `2a2a4fe` |
| `m3-integration` vs `main` | 34 | 0 | `main` is a strict ancestor |

**The count in this item's original body was `16`.** The handoff of 2026-08-11 said `17`. The
measurement above says `22`. Three values for one claim in two days is why the normative body now
carries none.

**`origin/*` figures above were taken without fetching**, deliberately, because fetch mutates
remote-tracking refs during a read-only measurement. They are therefore **not established**. Fetching
is part of the measurement boundary for any eventual push or `main` decision.

## The conflict that must be adjudicated first

`reconciliation/m3-integration` carries two items that do not exist on `m3-scope-registry`, both
children of EP-01:

```text
FE-14  A skipped oracle is not a passing integration       IN_PROGRESS
FE-15  Concurrent processes share a mutable cache root     READY
```

On `m3-scope-registry`, EP-01 was closed on 2026-08-12 as having had its pinned-identity guarantee
re-established by FE-13/`0.4.1`. Both accounts cannot stand: a parent cannot be `COMPLETE` over open
children, and FE-15's subject — whether concurrent materialisations can cause the consumed checkout
to stop proving the requested identity — belongs under EP-01's guarantee rather than beside it.

Adjudicated 2026-08-12: **EP-01 reopened on this line.** See EP-01's own history. FE-14's parentage is
a separate question, to be justified from its acceptance property rather than inherited.
