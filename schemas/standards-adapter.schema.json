{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://standards-enforcer/schemas/standards-adapter.schema.json",
  "title": "Standards adapter contract",
  "description": "How one standards pack declares the way it is invoked and the way its result is read. The pack owns its contract; StandardsEnforcer owns this schema. Declarative only — no code, no command semantics, no discovery. Every field below was forced by a real pack recorded in artifacts/evidence/2026-08-09-interface-inventory.md; a field nothing forced is absent, and adding one is a schemaVersion change.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "standard", "evaluation", "result"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": {
      "description": "Matched exactly. An unrecognised version is ENFORCEMENT_ERROR, never a best-effort read — the enforcer must not guess what a contract it does not understand is asking for.",
      "const": "1.0.0"
    },
    "standard": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "properties": {
        "id": {
          "description": "Stable identifier for the pack, used to name it in the enforcer's result. Not a display name and never reused.",
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        }
      }
    },
    "evaluation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["entrypoint", "arguments"],
      "properties": {
        "entrypoint": {
          "description": "Path to the CLI within the pack's own checkout. FORCED BY PredictionStandards, whose entry is scripts/predictions.mjs where every other pack uses scripts/standards.mjs.",
          "type": "string",
          "pattern": "^[^/\\\\][^\\\\]*$"
        },
        "arguments": {
          "description": "The authoritative verdict invocation, literally. FORCED BY the divergence in every dimension at once: the verdict subcommand is `validate` in Engineering, Betting, Mathematics and Innovation, `check` in Financial and Health, and `evaluate` in MachineLearning; the target is positional in some and `--dir=<path>` in others; and Financial takes `--policy <path>` space-separated where others use `=`. No aliasing, preference ordering or discovery may be inferred from this array — `check` exists in Betting and Innovation and answers a DIFFERENT question in both, executing cleanly and returning a verdict-shaped object, so a pack that does not name its verdict command literally cannot be invoked safely.",
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" },
          "contains": {
            "description": "At least one argument must carry the {target} placeholder, so a contract cannot declare an invocation that never says what to evaluate. Matched as a SUBSTRING, not as a whole argument: Betting passes the target positionally as `{target}` while MachineLearning, Mathematics and Innovation embed it as `--dir={target}`. This was written as `const` until 2026-08-10, which admitted only the positional form — a constraint that excluded three of the four released contracts and went unnoticed for as long as nothing executed the schema. See artifacts/evidence/2026-08-10-conformance-boundary-built.md.",
            "type": "string",
            "pattern": "\\{target\\}"
          }
        }
      }
    },
    "result": {
      "type": "object",
      "additionalProperties": false,
      "required": ["statuses", "passing"],
      "properties": {
        "statuses": {
          "description": "The pack's COMPLETE status vocabulary, read from the top-level `status` key of its JSON. Required so that an unrecognised status fails closed rather than being treated as merely not-passing: those are different problems. FORCED BY the vocabularies not being one alphabet — Engineering has four values and no BLOCKED_BY_INVARIANT, and PredictionStandards uses SUPPORTED / SUPPORTED_WITH_EXCEPTIONS / INSUFFICIENTLY_SUPPORTED.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "passing": {
          "description": "The closed subset of `statuses` on which a merge may proceed, declared by the pack. Present not because the packs disagree — all six provable packs pass on COMPLIANT and COMPLIANT_WITH_EXCEPTIONS — but because ADR 0001 forbids the enforcer holding the knowledge that COMPLIANT means passing. A status outside this set is not passing, including one outside `statuses` entirely.",
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  },
  "$absentByDesign": {
    "statusPath": "Every provable pack puts its verdict at the top-level `status` key, so nothing forced a path expression. PredictionStandards would have — and its integration is blocked for a different reason: it publishes no authoritative status at all.",
    "exitCodes": "Nothing forced it, and interpreting exit codes would mislead. Betting exits 2 on NOT_EVALUATED with valid JSON, MachineLearning exits 3 on blocked, Health exits 4 on NOT_EVALUATED, and Financial collapses blocked into 1. Parseable JSON carrying a declared status is the signal that the pack ran; the exit code is process semantics and the enforcer does not read it as a verdict.",
    "runtime": "All eight are Node. Nothing forced the field; the first non-Node pack forces it and a schemaVersion bump.",
    "workingDirectory": "Tested rather than assumed. No pack requires its own checkout as cwd — Financial ignores cwd entirely, Engineering and MachineLearning default to it and are given {target} explicitly regardless.",
    "output": "All eight write JSON to stdout under --json. Nothing forced a declaration.",
    "versionSource": "All eight carry a VERSION file and the enforcer reads it. Note that package.json disagrees with VERSION in Engineering, Financial and Innovation, so the choice matters — but no pack forces it to be declarative."
  }
}
